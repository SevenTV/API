type Image {
  name: String!
  format: ImageFormat!
  url: String!
  width: Int!
  height: Int!
  frame_count: Int!
  size: Int!
}

enum ImageFormat {
  AVIF
  WEBP
  GIF
  PNG
}

type Archive {
  name: String!
  content_type: String!
  url: String!
  size: Int!
}
